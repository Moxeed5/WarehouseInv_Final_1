@model Product

<h3 style="text-align: center;">@Model.Name Information</h3>

<style>
    .bg-color {
      background-color: #808080;
      opcaity: 1;
      font-size: 30px;
     }

      a {
     color: yellow;
     }

    .mytable {
        margin-bottom: 0px;
    }

</style>


<div class=" bg-color">

<table class="table">
    <tr>
        <th>UPC</th>
        <th>Name</th>
        <th>Price</th>
        <th>Category</th>
        <th>QTY</th>
    </tr>
    <tr>
        <td>@Model.UPC</td>
        <td>@Model.Name</td>
        <td>@Model.Price</td>
        <td>@Model.Category</td>
        <td>@Model.QTY</td>
    </tr>
</table>

<div style="display: block">
    <a href="/Product/UpdateProduct/@Model.UPC">Update this Product</a>
</div>

<div style="display: block">

    @using (Html.BeginForm("DeleteProduct", "Product", "Post"))
    {
        @*<input type="hidden" asp-for="UPC" value="@Model.UPC" />
        <input type="submit" onclick="return confirm('Are you sure?')" value="Delete this product" />*@

            <input type="submit" class="btn btn-danger" onclick="return confirm('Are you sure?')" value="Delete this product" />

    }
</div>
